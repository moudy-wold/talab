import React from 'react'
type Params = {
    params: {
      locale: string;
      slug: string;
    };
  };

function Page({ params: { locale,slug } }:Params) {
  return (
    <div>
      
    </div>
  )
}

export default Page
